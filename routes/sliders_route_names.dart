class SlidersRouteNames {
  static const String seeAllPath = '/sliders/see-all';

  // For backward compatibility or if the user prefers this syntax
  static String seeAll(String title, [dynamic sliders]) => seeAllPath;
}
